/**
 * Glassy macOS Clipboard Theme
 * Precise Match to Launcher Image
 */

configuration {
    modi:                       "drun";
    show-icons:                 false;
    font:                       "JetBrainsMono Nerd Font 10";
}

@import "colors.rasi"

* {
    bg-color:            @background;
    bg-selected:         @primary-container;
    fg-color:            @on-surface;
    accent-color:        @primary;
    border-color:        @outline-variant;
}

window {
    width:                       400px;
    spacing:                     0px;
    padding:                     0px;
    background-color:            @bg-color;
    border:                      1px;
    border-color:                @border-color;
    border-radius:               12px; /* Lebih bulat sesuai gambar */
    cursor:                      "default";
    transparency:                "real";
    location:                    center;
    anchor:                      center;
}

mainbox {
    padding:                     10px;
    background-color:            transparent;
    children:                    [ "inputbar", "listview" ];
}

inputbar {
    spacing:                     10px;
    margin:                      0px 0px 5px 0px;
    padding:                     8px 10px;
    border-radius:               6px;
    background-color:            #ffffff0d;
    text-color:                  @fg-color;
    children:                    [ "prompt", "entry" ];
}

prompt {
    enabled:                     true;
    background-color:            transparent;
    text-color:                  @accent-color;
    content:                     ""; /* Ikon Search sesuai gambar */
}

entry {
    padding:                     0px;
    background-color:            transparent;
    text-color:                  inherit;
    placeholder:                 "Search...";
    placeholder-color:           #ffffff4d;
}

listview {
    columns:                     1;
    lines:                       8;
    cycle:                       true;
    dynamic:                     true;
    scrollbar:                   false;
    layout:                      vertical;
    reverse:                     false;
    fixed-height:                true;
    fixed-columns:               true;
    spacing:                     6px; /* Jarak antar list sedikit lebih renggang */
    background-color:            transparent;
    border:                      0px;
}

element {
    spacing:                     10px;
    padding:                     8px 12px;
    border-radius:               6px;
    background-color:            transparent;
    text-color:                  @fg-color;
}

element normal.normal {
    background-color:            transparent;
    text-color:                  @fg-color;
}

element alternate.normal {
    background-color:            transparent;
    text-color:                  @fg-color;
}

element selected.normal {
    background-color:            @bg-selected;
    text-color:                  @accent-color;
    border:                      1px;
    border-color:                #ffffff1a;
}

element-text {
    background-color:            transparent;
    text-color:                  inherit;
    vertical-align:              0.5;
}
